# 开发清单

## 待办清单

### 缓存区

- [ ] LinkCard左侧添加一个垂直居中的可供拖拽的按钮。支持拖拽到分组、标签或特别关注里，也支持在页面内对链接进行排序

- [ ] 新增功能：快捷搜索
```markdown
新增类似mac里spotlight一样的全局搜索框，分配一个新的快捷键（默认前缀+K），搜索框支持搜索和添加连接。
- 按esc键或失去焦点时退出。
- 搜索功能：搜索出的结果只展示标题、分组、标签，点击可以跳转
- 添加链接：当搜索框内出现url时候，展示添加链接的
```

- [ ] 重构代码，审视冗余代码，抽取常量

### 工作区



### 缺陷修复

### 新功能（待定）

- [ ] 支持从分组或标签创建链接，自动将对应的分组和链接带到新的链接上

- [ ] 新增浏览器插件，直接从浏览器按钮一件添加链接
  - 特殊操作：Native Messaging (连接原生本地应用)
    - 如果插件需要自由读写本地任意位置的文件，普通的扩展机制（JS）是做不到的。插件必须通过 nativeMessaging 机制与一个安装在本地的“原生应用”通信，由该原生应用执行高权限的文件写入操作。
- [ ] 支持树形结构的分组
- [ ] 支持导出成浏览器支持导入的html文件（分组对应到）

- [ ] 支持导入 JSON / CSV / markdown 格式的链接

- [ ] 花里胡哨的总结性视图

- [ ] 自动备份（定期导出数据库备份）
- [ ] 支持导入 JSON / CSV / markdown 格式的链接

### 体验优化（待定）

- [ ] 鼠标挪到分组和标签上时，预览数量
- [ ] 搜索历史记录
- [ ] 链接卡片显示创建时间
- [ ] 列表视图 / 卡片视图切换
- [ ] 空状态引导优化（分组为空、搜索无结果时的提示）
- [ ] 加载骨架屏（替代 spinner）
- [ ] 表单输入体验优化（Tab 键顺序、Enter 提交）
- [ ] 侧边栏分组和标签数量统计

### 技术债务（待定）

- [ ] 添加单元测试（Rust 后端）
- [ ] 添加 E2E 测试（前端）
- [ ] 数据库迁移脚本（替代 `CREATE TABLE IF NOT EXISTS`，支持 schema 版本管理）
- [ ] 错误边界处理（前端全局错误捕获和用户友好提示）
- [ ] 日志系统完善（前端错误上报到 Rust 侧统一记录）
- [ ] 性能监控（链接列表渲染耗时、搜索耗时）
- [ ] 用户指导文档编写

## 开发日志

### 2026-04-27

- [x] 当用户对LinkCard做操作之后，现在偶发会随机移动滚轮的位置，导致用户找不到北了。对LinkCard做的所有操作，标记、编辑、删除等，都应该尽可能不改变滚轮位置（有例外场景，但也应该保证滚轮的移动幅度最小）
- [x] 新增快捷键（默认前缀Windows ctrl + shift；Mac cmd + shift）J 用于唤起主程序，并添加相应的快捷键配置

- [x] 给LinkCard搜索结果下放一个空的card，防止数据较多时，最后一个链接的按钮被新增链接按钮遮住
- [x] 提供开机自动启动功能和配置选项
- [x] 主题暗色或者亮色支持跟随系统
- [x] 更改添加链接时，分组和标签的交互形式。用户选中输入框时，提供一个固定高度但是可以滑动的下拉框

- [x] 快捷添加链接的窗口没有跟随系统颜色
- [x] 编辑和添加链接时，如果没有标签，点击标签输入框时出现暂无标签下拉框（跟分组保持一致）

### 2026-04-26

- [x] 卡片链接上的分组和标签快捷删除

- [x] 编辑链接窗口有多个问题
  - [x] 点击删除标签有概率直接关闭编辑链接的窗口 —— 根因：TagInput 删除按钮缺 `type="button"`，默认 submit 触发表单提交
  - [x] 如果在页面内按住鼠标左键不放，然后拖动到页面外放开鼠标左键，页面也会自动关闭 —— 根因：click 事件在拖拽释放时 target 是 mouseup 位置（overlay），导致误判；改为 mousedown 记录来源，click 时检查区分
  - [x] 在url和标题的输入框内回车，会直接删除一个标签并关闭编辑链接 —— 根因：Enter 触发表单提交，为输入框添加 `onkeydown` 阻止 Enter 默认行为
- [x] 支持在Sidebar修改分组和标签的名称 —— 添加编辑按钮，点击后名称可编辑，按回车保存，Esc 取消，保存后自动刷新链接卡片
- [x] 未分组没有跟下面的分组对齐 —— `padding-left: 10px` → `12px`，与顶级分组一致
- [x] LinkCard 列表底部添加空白间距，防止最后一条被FAB遮挡
- [x] 提供开机自动启动功能和配置选项 —— tauri-plugin-autostart，设置面板「通用」开关
- [x] 主题支持跟随系统外观 —— 设置面板新增「外观」选项（亮色/暗色/跟随系统），兼容旧版 dark-mode 配置
- [x] 设置面板小窗口下支持滚动 —— modal-body 可滚动，footer 固定底部
- [x] TagInput 支持下拉创建不存在的标签

### 2026-04-25

- [x] 有些情况日志没有追加到Links.log，而是每次都覆盖
- [x] 完善fetch_metadata的逻辑
  - [x] 继续扩大截取的范围，防止因截取获取不到信息
  - [x] 请求走系统代理
  - [x] 在日志中打印完成的出入参和详细报错堆栈

- [x] 当前配置只记忆了窗口大小，没记忆窗口位置
- [x] mac系统下，第一次打开quickAdd是方角而不是圆角；
- [x] mac系统的托盘，点击的时候没有直接打开程序，而是弹出选项；弹出选项应该是右键唤起的
- [x] sidebar分组和标签新增固定的"未分组"和"没标签"筛选条件

### 2026-04-24 (4)

- [x] 将特别关注改为书签样式的图标（图标风格跟其他图标保持一致），具体的交互逻辑改为：当点击该图标后，linkcard右上角标记上黄色的书签，被标记的link，平常书签图标不展示，触碰后颜色变为书签的颜色，再次点击图标取消标记后，图标变回原来的颜色
- [x] mac下 cmd + tab 切换应用时，Links的图标要大一圈
  - [x] mac是正常了，但是windows又小了一圈

### 2026-04-24 (3)

- [x] 在添加链接时，就算已经抓取成功，点击保存后还会抓取一次
- [x] 链接卡片的按钮应该上下居中
- [x] 重新审视一下所有地方的样式是否统一和谐，将识别出来的不符合要求的地方列举出来
  - **① 按钮样式重复定义 4 处**：`.btn` / `.btn-primary` / `.btn-secondary` 在 `LinkForm`、`ExportDialog`、`SettingsDialog` 各写一遍（完全相同），`LinkCard` 的 `.confirm-btn` 也是同一套样式，`App.svelte` 的 `.close-btn` 同理 → 建议提取到 `app.css` 全局样式
  - **② 模态框样式重复 3 处**：`.modal-overlay`、`.modal`、`.modal-header`、`.modal-title`、`.modal-close` 在 `LinkForm`、`ExportDialog`、`SettingsDialog` 各写一遍（完全相同） → 同上，提取到全局
  - **③ Sidebar 标签字号比分组小**：`.cat-item` 是 `13px`，`.tag-item` 是 `12px`，两者并列视觉不统一 → 建议统一为 `13px`
  - **④ Sidebar 底栏按钮 hover 无变化**：`.sidebar-footer` 背景 `var(--bg-2)`，`.footer-btn:hover` 也是 `var(--bg-2)` → 建议改为 `var(--bg-hover)` 或 `var(--border-1)`
  - **⑤ LinkCard `.card-title-row` 定义了两次**：line 250-257 和 line 306-310 是同一个选择器的两个块 → 应合并
  - **⑥ 硬编码颜色代替 CSS 变量**：`.cat-chip` 用 `#f0fdf4`/`#15803d`（等于 `--cat-soft`/`--cat-text`），`.note-chip` 用 `#fef2f2`/`#dc2626`，`.copy-toast` 用 `#16a34a`，`.active-fav`/`.broken-badge` 用 `#f59e0b`，`.dup-warning` 用 `#d97706` → 建议统一使用 CSS 变量
  - **⑦ 输入框 focus ring 不一致**：Sidebar `.section-search-input:focus` 用 `2px`，其余所有输入框用 `3px` → 建议统一 `3px`
  - **⑧ `App.svelte` 的 `.close-overlay` 缺少 `-webkit-backdrop-filter`**：其他 3 个 overlay 都有，Safari 兼容性需要
  - **⑨ Sidebar 标签删除提示复用了 `.cat-delete-hint`**：标签条目里的"再点一下~"用的是 `.cat-delete-hint`（分类的类名） → 建议改为通用命名如 `.delete-hint`

### 2026-04-24 (2)

- [x] CI/CD 配置（GitHub Actions 自动构建 macOS / Windows 安装包）
- [x] 如果直接把db等文件删除，mac下重新打开程序可以正常运行，windows程序会卡死
- [x] 启动时后应该给一个加载的动画；mac现在是一片空白，windows是一个透明的窗口

### 2026-04-24 (1)

- [x] 链接分享（复制到剪贴板，支持 URL / Markdown / HTML 三种格式）
- [x] 链接去重检测（URL 重复时提示，编辑时排除自身）
- [x] 链接失效检测（添加时自动检测，卡片显示警告图标）

### 2026-04-23 (3)

- [x] 配置迁移为独立 config.json 文件（与 links.db 同目录），日志也改为同一目录
- [x] 窗口的大小仍然没有记忆
- [x] 新建链接时候智能总结标签
- [x] 备注的图标风格跟分组和标签保持一致

### 2026-04-23 (2)

- [x] 每次询问功能不生效；打开设置的时候，选项会从第一行闪烁到用户的选择；用户对客户端大小的设置没有记忆住（持久化到配置中）
- [x] 在链接卡片中的分组和标签前也加上图标，跟Sidebar里的保持一致
- [x] 已有的链接不能编辑url了

### 2026-04-23 (1)

- [x] 添加标签和分组时，确定和取消的按钮位置不自然，应该自然填充整行而不是缩在左边；此外，分组的新增应该放到第一行，跟标签保持一致
- [x] 删除标签和分组的交互逻辑不对，现在是用户只要点击了一次删除按钮，这个状态就被保存了，用户鼠标移走再回来点击一次就能删除。正确的交互逻辑应该是用户触碰上删除按钮后，点击第一次在左边提示"再点一下"，挪走鼠标后应恢复原状
- [x] hint改成仅当内容被缩略的时候可以触碰展示
- [x] 添加和编辑链接的时候，应该用编辑框内的颜色，提示用户内容是否可编辑。
- [x] 把设置按钮放在Sidebar Links那一行的右边
- [x] 在链接卡片展示分组和标签的地方，用相同的风格，红色的色系，展示备注的内容
- [x] 侧边栏选定标签的时候全部链接也高亮了
- [x] 将"个人备注.." 改为 "添加你的想法吧~"，同时识别一下所有的提示语，都改成有温度，带一点俏皮的语气
- [x] 长标题 / 长描述在链接卡片中被截断，无法展开查看完整内容，触碰到长标题和长描述时候用hint展示；触碰到域名部分在原地展开完整url
  - [x] hint刷新太久才出来，且样式很丑，风格跟整体不统一；展开的url，同样感觉风格没统一，还导致了链接卡片的宽度变化；搜索展示高亮内容时，如果url是匹配的，则也展开
    - [x] hint应该一碰到标题或者描述就刷新出来（现在好像完全不加载了）；展开url的字体颜色等样式应该和展开前保持一致 
- [x] 编辑链接时支持重新抓取元数据（刷新按钮）
  - [x] 新增链接的时候也要有这个刷新按钮
- [x] 暗色模式下侧边栏选中标签和全部链接的颜色不一样，我希望标签取的是全部链接的颜色；另外暗色模式下分组的绿色偏亮偏深了，参考全部链接在暗色模式下的亮度和饱和度
- [x] 提供常用的配置项（如关闭程序是托盘还是退出）
  - [x] 功能实现不正确。应该把配置也用json持久化到db的位置
- [x] 程序第一次启动应该默认是最小的，用户可以根据需要调整大小，这个也隐藏在配置里

- [x] 系统托盘（最小化到托盘，后台运行）
- [x] 支持导入书签（Chrome / Firefox / Safari 书签 HTML）
- [x] 重构侧边栏分组和标签管理的样式，条目颜色与链接卡片上保持一致（蓝色、绿色）
- [x] macos下下拉或者上划会出现白条，Windows下是没有的，应该固定住保持一致
