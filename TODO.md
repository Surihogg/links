# 开发清单

## 工作台

### 缓存区

- [ ] 快捷键功能改为类似spotlight一样的全局搜索框，搜索框支持搜索和添加连接
- [ ] 链接支持拖拽到分组和标签和特别关注里，我设想的交互模式是

### 工作区

### 已完成

#### 2026-04-23 (3)

- [x] 配置迁移为独立 config.json 文件（与 links.db 同目录），日志也改为同一目录
- [x] 窗口的大小仍然没有记忆
- [x] 新建链接时候智能总结标签
- [x] 备注的图标风格跟分组和标签保持一致

#### 2026-04-23 (2)

- [x] 每次询问功能不生效；打开设置的时候，选项会从第一行闪烁到用户的选择；用户对客户端大小的设置没有记忆住（持久化到配置中）
- [x] 在链接卡片中的分组和标签前也加上图标，跟Sidebar里的保持一致
- [x] 已有的链接不能编辑url了

#### 2026-04-23 (1)

- [x] 添加标签和分组时，确定和取消的按钮位置不自然，应该自然填充整行而不是缩在左边；此外，分组的新增应该放到第一行，跟标签保持一致
- [x] 删除标签和分组的交互逻辑不对，现在是用户只要点击了一次删除按钮，这个状态就被保存了，用户鼠标移走再回来点击一次就能删除。正确的交互逻辑应该是用户触碰上删除按钮后，点击第一次在左边提示"再点一下"，挪走鼠标后应恢复原状
- [x] hint改成仅当内容被缩略的时候可以触碰展示
- [x] 添加和编辑链接的时候，应该用编辑框内的颜色，提示用户内容是否可编辑。
- [x] 把设置按钮放在Sidebar Links那一行的右边
- [x] 在链接卡片展示分组和标签的地方，用相同的风格，红色的色系，展示备注的内容
- [x] 侧边栏选定标签的时候全部链接也高亮了
- [x] 将"个人备注.." 改为 "添加你的想法吧~"，同时识别一下所有的提示语，都改成有温度，带一点俏皮的语气
- [x] 长标题 / 长描述在链接卡片中被截断，无法展开查看完整内容，触碰到长标题和长描述时候用hint展示；触碰到域名部分在原地展开完整url
  - [x] hint刷新太久才出来，且样式很丑，风格跟整体不统一；展开的url，同样感觉风格没统一，还导致了链接卡片的宽度变化；搜索展示高亮内容时，如果url是匹配的，则也展开
    - [x] hint应该一碰到标题或者描述就刷新出来（现在好像完全不加载了）；展开url的字体颜色等样式应该和展开前保持一致 
- [x] 编辑链接时支持重新抓取元数据（刷新按钮）
  - [x] 新增链接的时候也要有这个刷新按钮
- [x] 暗色模式下侧边栏选中标签和全部链接的颜色不一样，我希望标签取的是全部链接的颜色；另外暗色模式下分组的绿色偏亮偏深了，参考全部链接在暗色模式下的亮度和饱和度
- [x] 提供常用的配置项（如关闭程序是托盘还是退出）
  - [x] 功能实现不正确。应该把配置也用json持久化到db的位置
- [x] 程序第一次启动应该默认是最小的，用户可以根据需要调整大小，这个也隐藏在配置里

- [x] 系统托盘（最小化到托盘，后台运行）
- [x] 支持导入书签（Chrome / Firefox / Safari 书签 HTML）
- [x] 重构侧边栏分组和标签管理的样式，条目颜色与链接卡片上保持一致（蓝色、绿色）
- [x] macos下下拉或者上划会出现白条，Windows下是没有的，应该固定住保持一致

## 新功能（待定）

- [ ] 链接、分组支持拖拽排序
- [ ] 链接去重检测（URL 重复时提示）
- [ ] 链接失效检测（定期检查 URL 是否可达）
- [ ] 支持导入 JSON / CSV / markdown 格式的链接
- [ ] 链接备注支持 Markdown 渲染
- [ ] 侧边栏宽度可拖拽调整
- [ ] 键盘快捷键（删除、编辑、搜索聚焦等）
- [ ] 多选模式（Shift/Ctrl 批量选中）
- [ ] 链接分享（生成短链或复制富文本）
- [ ] 支持自定义快捷键
- [ ] 自动备份（定期导出数据库备份）

## 体验优化（待定）

- [ ] 链接列表虚拟滚动（大量链接时保持流畅）
- [ ] 侧边栏分组支持右键菜单（重命名、删除、新建子分组）
- [ ] 搜索支持快捷键聚焦（Cmd/Ctrl+K）
- [ ] 搜索历史记录
- [ ] 链接卡片显示创建时间
- [ ] 列表视图 / 卡片视图切换
- [ ] 空状态引导优化（分组为空、搜索无结果时的提示）
- [ ] 加载骨架屏（替代 spinner）
- [ ] 表单输入体验优化（Tab 键顺序、Enter 提交）
- [ ] 侧边栏分组和标签数量统计

## 技术债务（待定）

- [ ] 添加单元测试（Rust 后端）
- [ ] 添加 E2E 测试（前端）
- [ ] 数据库迁移脚本（替代 `CREATE TABLE IF NOT EXISTS`，支持 schema 版本管理）
- [ ] 错误边界处理（前端全局错误捕获和用户友好提示）
- [ ] 日志系统完善（前端错误上报到 Rust 侧统一记录）
- [ ] 性能监控（链接列表渲染耗时、搜索耗时）
- [ ] CI/CD 配置（GitHub Actions 自动构建 macOS / Windows 安装包）
